@model Notifications.Web.Models.LoginModel

@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@if (User.Identity.IsAuthenticated)
{
    
}

@using (Html.BeginForm())
{

    @Html.EditorForModel()
    <input type="submit" class="btn" value="Login" />
}